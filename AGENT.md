# 项目偏好与环境约定

用于后续修改、协作与统筹规划的统一偏好说明。

## 1) 语言偏好

- 默认使用中文进行沟通与说明。
- 代码注释优先简洁中文；仅在必要时补充英文术语。

## 2) Python/运行环境

- 统一使用 Conda 环境：`mujoco_diffusion`
- 建议每次进入项目后先执行：

```bash
conda activate mujoco_diffusion
```

## 3) 开发与执行建议

- 在该环境下运行主程序：

```bash
python main.py
```

- 若新增依赖，优先记录到项目依赖文件（如 `requirements.txt` 或 `environment.yml`）。

## 4) 维护约定

- 进行功能修改前，先简要列出目标、影响文件与验证步骤。
- 修改完成后，记录：
  - 改动文件
  - 关键逻辑变化
  - 运行/验证结果
